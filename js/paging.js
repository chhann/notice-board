let totalCount = "총 게시글 수";

let listCount = 5

let totalPage = "총페이지수";

// totalPage = totalCount / listCount;
// if(totalCount % listCount > 0) {
//    totalPage++;
// } 

let pageCount = "한 화면에 출력될 페이지네이션 개수";
startPage = ((curPage-1)/pageCount)*pageCount + 1;

